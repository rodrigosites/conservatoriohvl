<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>Cliente:</th>
      <th>Endereço:</th>
      <th>RG:</th>
      <th>CPF:</th>
      <th>Telefone:</th>
      <th>Celular:</th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @cliente.nome %></td>
        <td><%= @cliente.endereco %></td>
        <td><%= @cliente.rg %></td>
        <td><%= @cliente.cpf %></td>
        <td><%= @cliente.telefone %></td>
        <td><%= @cliente.celular %></td>
      </tr>
  </tbody>
</table>

<% @alunos = get_clientes(@cliente.id) %>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Alunos Vinculados:</th>
      <th>Curso:</th>
      <th>Valor Mensal:</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @alunos.each do |aluno| %>
      <tr>
        <td><%= aluno.nome %></td>
        <td>INCLUIR NOME CURSO</td>
        <td>INCLUIR VALOR MENSAL</td>
        <td><%= link_to 'Mostrar', aluno_path(aluno) %></td>
        <td><%= link_to 'Editar', edit_aluno_path(aluno) %></td>
        <td><%= link_to 'Excluir', aluno, method: :delete, data: { confirm: "Tem certeza que deseja excluir o aluno #{aluno.nome}?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Editar Cliente', edit_cliente_path(@cliente) %> |
<%= link_to 'Voltar', clientes_path %>
