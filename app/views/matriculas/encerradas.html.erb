<h1>Lista de Matrículas Inativas</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Aluno</th>
      <th>Curso</th>
      <th>Ano</th>
      <th>Professor</th>
      <th>Horário</th>
      <th>Valor Mensal</th>
      <th>Data da Matrícula</th>
      <th>Data de Encerramento</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @matriculas.each do |matricula| %>
      <tr>
        <td><%= matricula.aluno.nome %></td>
        <td><%= matricula.curso.nome %></td>
        <td><%= matricula.ano %>º Ano</td>
        <td><%= matricula.horario.professor.nome %></td>
        <td><%= matricula.horario.horario.to_s.slice(10..15) %></td>
        <td><%= matricula.valor_mensal %></td>
        <td><%= matricula.data_matricula.to_s.slice(0..9).to_date.to_formatted_s(:rfc822) %></td>
        <td><%= matricula.termino_matricula.to_s.slice(0..9).to_date.to_formatted_s(:rfc822) %></td>
        <td><%= button_to 'Mostrar Perfil', matricula, :class => "btn btn-default btn-xs", :method => :get %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<b>Total de matrículas inativas: <%= @matriculas.size %></b>

<br /><br />

<%= button_to 'Cadastrar Nova Matrícula', new_matricula_path, :class => "btn btn-default", :method => :get  %>
