<%= form_for(@disponibilidade, html: { class: "form-horizontal", "role" => "form" }) do |f| %>
  <% if @disponibilidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disponibilidade.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @disponibilidade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.hidden_field :professor_id, value: "#{params[:professor_id]}" %>
    <%= f.label "Dia:", class: "col-sm-0 control-label" %><br />
    <%= f.select :dia, ["Segunda","Terça","Quarta","Quinta","Sexta","Sábado" ] , { include_blank: true }, class: "form-control" %>
    <%= f.label "Horário:", class: "col-sm-0 control-label" %><br />
    <%= f.time_field :horario, class: "form-control" %>
  </div>
  <div class="form-group">
    <br /><%= f.submit "Cadastrar", class: "btn btn-default" %>
  </div>
<% end %>
