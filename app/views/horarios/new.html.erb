<div class="page-header">
  <h2>Cadastrar Novo Horário</h2>
</div>

<%= render 'form' %>

<%= button_to 'Voltar', professor_path(params[:professor_id]), :class => "btn btn-primary", :method => :get  %>
